@import "../styles/index";

$gridPrefixCls = $prefix + '-grid';
$flexPrefixCls = $prefix + '-flexbox';

.{$gridPrefixCls} {
	position: relative;
	overflow: hidden;
	hairline('top');
	.{$flexPrefixCls} {
		background: $background-color-base;
	}
}
