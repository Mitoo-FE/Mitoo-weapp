<view class="mit-panel">
    <view wx:if="{{ title }}" class="mit-panel-title">{{title}}</view>
    <view class="mit-panel-info {{ withoutBorder ? 'mit-panel-info-without-border' : '' }}">
        <slot></slot>
    </view>
</view>

