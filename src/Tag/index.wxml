<view bind:tap="select" style="{{size ? 'font-size:' + size + 'rpx;': ''}} color: {{borderColor? borderColor: ''}};border-color:{{borderColor ? borderColor : ''}}; background-color:{{backgroundColor ? backgroundColor : ''}}" class="mit-tag">
    <slot></slot>
</view>
