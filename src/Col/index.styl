@import "../styles/default";

.mit-col {
    float: left;
    box-sizing: border-box;
    width: 0;
}

genColStyle(num) {
	.mit-col-{num} { width: ( num * 100% / $mit-column)  }
	.mit-col-offset-{num} { margin-left: (num * 100% / $mit-column)}
}

for $i in 1..$mit-column{
	genColStyle($i)
}
